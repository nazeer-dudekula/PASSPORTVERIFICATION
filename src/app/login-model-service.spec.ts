import { LoginModelService } from './login-model-service';

describe('LoginModelService', () => {
  it('should create an instance', () => {
    expect(new LoginModelService()).toBeTruthy();
  });
});
